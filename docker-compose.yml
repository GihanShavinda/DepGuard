# DepGuard — service orchestration
# Services are filled in as they are built, step by step.
#
#   parser : Express  (added in Step 2)
#   api    : Laravel  (added in Step 3)
#   db     : database (added in Step 3)
#   web    : Angular  (added in Step 5)

services:
  # ── parser: Express JS-ecosystem microservice ──
  # (uncomment once parser/Dockerfile exists — Step 2)
  #
  # parser:
  #   build: ./parser
  #   ports:
  #     - "${PARSER_PORT:-3001}:3001"
  #   environment:
  #     - PARSER_PORT=3001

  # ── api: Laravel brain (auth, queue, orchestration) ──
  # (added in Step 3)

  # ── db: database ──
  # (added in Step 3)

  # ── web: Angular dashboard ──
  # (added in Step 5)
