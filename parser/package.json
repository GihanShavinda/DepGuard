{
  "name": "depguard-parser",
  "version": "0.1.0",
  "description": "DepGuard parser service — turns JS lockfiles into a flat dependency list and runs heuristic Trust Score signals.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "fastest-levenshtein": "^1.0.16"
  }
}
